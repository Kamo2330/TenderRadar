# Django
DJANGO_SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Optional PostgreSQL (leave unset for local SQLite):
# DATABASE_URL=postgres://user:password@localhost:5432/tenderradar
